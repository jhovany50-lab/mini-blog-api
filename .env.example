# ======================================
# Configuración del servidor
# ======================================
PORT=3000

# ======================================
# Base de datos local (PostgreSQL)
# ======================================
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=tu_password
DB_NAME=blog_db

# ======================================
# Producción (Railway / Render)
# Si DATABASE_URL está definida, la API
# la utilizará automáticamente.
# ======================================
DATABASE_URL=